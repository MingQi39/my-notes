---
date: 2026-03-19 21:10
modified: 2026-03-22 20:41
---

# 模型
## 在线模型

| 名字         | 建议购买的地址                               | 特点                |
| ---------- | ------------------------------------- | ----------------- |
| 百炼平台       | https://bailian.console.aliyun.com/   | 国内的模型很全面          |
| 智普AI       | https://open.bigmodel.cn/             | 最新的4.5和4.6，还有搜索工具 |
| Xiaoai中转   | https://xiaoai.plus/register?aff=3Tlp | 国外的模型很丰富          |
| Kimi（月之暗面） | https://platform.moonshot.cn/         | 有潜力，有搜索工具         |

## 私有化部署

| 框架名称 | 所属团队 | 核心特点 |
| --- | --- | --- |
| VLLM | 加州大学伯克利分校 | 高效管理KV缓存，显存利用率可达95%以上；支持张量并行和流水线并行 |
| sGLang | 伯克利团队 | 利用Radix树高效管理和复用KV缓存前缀，尤其擅长多轮对话；结构化输出，零开销批处理；缓存感知负载均衡 |
| transformers库 | Hugging Face | 提供从模型加载、微调到推理的全流程；灵活的API和训练接口，支持模型范围最广 |
| LMDeploy | 上海人工智能实验室 | 量化部署工具链，针对华为昇腾等国产芯片优化、多模态融合支持 |
| TensorRT-LLM | NVIDIA | 深度链路优化，预编译优化，支持FP8/INT4等多种量化方案，内核级优化（针对Transformers模块的深度CUDA优化） |

## 算力云服务器

| 名字 | 算力云的地址 | 特点 |
| --- | --- | --- |
| 再造云 | https://gpu.zaizao.cloud | 最便宜，远程用；定制化服务 |
| Autodl算力 | https://www.autodl.com/home | 资源最多，远程用；隧道公交 |
| 阿里云 | https://account.aliyun.com/ | 最稳定，最贵，什么都有 |
| Suanli算力 | https://www.suanlix.cn/ | 有独立IP，远程用；隧道命令 |

> 建议：都要自己基于conda创建虚拟环境。而且虚拟环境和模型文件都要放到数据盘里面

![[Pasted image 20260319214930.png]]

管理 python 虚拟环境和 python 包：conda
conda 命令：
`conda config --add envs_dirs /root/autodl-tmp/envs`   配置目录
`conda config --show envs_dirs`  检查
`conda create --name ocr_env python=3.12` 创建一个名字叫xxx，版本是xxx的Python虚拟环境
`conda activate ocr__env`  进入到xxx的虚拟环境中
`conda install pip`  可选
`conda list`
`python -m pip install vllm0.9.1`
`python -m pip install -r requirements.txt`

系统盘不能扩充，数据盘空间不足，随时可扩充，因此，自己的数据尽量放到数据盘中；但是，conda 默认使用的是系统盘，因此，需要修改配置目录

# 概念

## Agent （智能体）
将语言模型与工具相结合，创建可以推理任务、决定使用哪些工具并==迭代==寻求解决方案的系统。

1. **模型调用**- 使用提示和可用工具调用 LLM，返回响应或执行工具的请求。
2. **工具执行**- 执行 LLM 请求的工具，返回工具结果。

这个循环一直持续到 LLM 决定完成为止

![[Pasted image 20260320084022.png]]

## Middleware（中间件）
中间件提供了一种更严格地控制 Agent 内部发生的事情的方法。中间件在每个步骤之前和之后都可以设置钩子。

- 监控：通过日志记录、分析和调试跟踪代理行为
- 修改：转换提示、工具选择和输出格式。
- 控制：添加重试、回退和提前终止逻辑。
- 其他：应用速率限制、护栏和 PII 检测

## Guardrails（护栏）
为您的代理实施安全检查和内容过滤。帮助您构建安全、合规的 AlI 应用程序。他们可以检测敏感信息、执行内容策略、验证输出并在不安全行为引起问题之前防止它们。

- 防止 PIl 泄漏（个人敏感信息拦截）
- 检测和阻止提示注入攻击（智能体安全问题）
- 屏蔽不当或有害内容
- 执行业务规则和合规性要求
- 验证输出质量和准确性

## 上下文工程
上下文工程是解决智能体的相应速度和提高智能体准确率的最重要的解决方案

LLM 失败的原因有以下两个：
1. 底层 LLM 能力不够；
2. “正确”的上下文没有传递给 LLM。

上下文工程是以正确的格式提供正确的信息和工具，以便 LLM 能够完成任务。这是人工智能工程师的头号工作。这种缺乏“正确”上下文是更可靠代理的头号障碍，而 LangChain 的代理抽象经过独特设计，以促进上下文工程。

# LangChain 

## 安装
[官网](LangChain) 

![[Pasted image 20260320090157.png]]

## LangChain V1 新特性

### Agent 架构

LangChain 1.0 的 Agent 是构建在 LangGraph 之上，提供以下功能：

- 持久执行
- 流式输出
- 人机交互
- 持久性

> 💡 您无需了解 LangGraph 即可使用基本的 LangChain 代理

LangChain 里面的agent 写法：项目不是很复杂
LangGraph 里面的定制逻辑链的写法。项目非常复杂

##  包安装 & API KEY 配置

```bash
pip install -U langchain-openai
pip install dotenv 
```

```.env
OPEN_API_KEY=xxx
DEEPSEEK_API_KEY=xxx
```

读取环境变量

```python env_utils.py
import os
load_dotenv(override=True)

OPEN_API_KEY = os.getenv('OPEN_API_KEY')
DEEPSEEK_API_KEY = os.getenv('DEEPSEEK_API_KEY')
```

使用本地部署的模型

```python
from langchain_openai import ChatOpenAI
from pydantic import SecretStr

llm = ChatOpenAI(
	model="qwen3.5:latest",
	temperature=0.5,
	base_url="http://localhost:11434/v1",
	api_key=SecretStr("ollama"),
)

response = llm.invoke("你好")
print(response.content)
```

